import 'package:flutter/material.dart';

class AdicionarTaskModal extends StatelessWidget {
  const AdicionarTaskModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
